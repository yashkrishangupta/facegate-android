<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera access for face capture -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Network access for backend sync -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WorkManager background sync needs this on Android 12+ -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name="com.facegate.FaceGateApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceGate">

        <!--
            Single entry point. NavigationActivity and StudentNavigationActivity
            have been merged into MainActivity — both nav graphs are loaded here
            at runtime depending on the role the user selects.
        -->
        <activity
            android:name="com.facegate.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            WorkManager auto-initializes itself via this ContentProvider,
            BEFORE Application.onCreate() runs — which is also before Hilt
            has injected FaceGateApp.workerFactory. That race causes
            WorkManager to silently fall back to a default (non-Hilt)
            configuration, which is why AttendanceSyncWorker — a
            constructor-injected @HiltWorker — fails with
            NoSuchMethodException at runtime even though everything compiles
            and the Hilt bindings are generated correctly. Removing this
            entry disables that auto-init; WorkManager then only initializes
            when FaceGateApp.onCreate() explicitly calls
            WorkManager.getInstance(context) (via
            AttendanceSyncWorker.Scheduler.schedulePeriodicSync), which is
            after super.onCreate() has completed Hilt's injection.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>